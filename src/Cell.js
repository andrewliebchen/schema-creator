import styled from "styled-components";

const Cell = styled.div`
  flex: 0 0 auto;
  width: 200px;
  padding: 8px;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
`;

export default Cell;
